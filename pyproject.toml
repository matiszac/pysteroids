[project]
name = "pysteroids"
version = "0.1.0"
description = "A simple asteroids clone writtin in python(pygame)."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame==2.6.0",
]
